/**
 * Default storage key prefix
 */
export const STORAGE_KEY_PREFIX = '@devkit-console';

/**
 * Default max history size
 */
export const DEFAULT_MAX_HISTORY_SIZE = 500;

/**
 * Version string (injected at build time)
 */
export const VERSION = '__DEVKIT_VERSION__';
